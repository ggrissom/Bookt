import { login } from "./actions";
export default function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) {
  return <main className="mx-auto flex min-h-screen max-w-md items-center p-6"><form action={login} className="w-full rounded-2xl border border-stone-300 bg-white p-7 shadow-sm"><p className="mb-2 text-sm font-semibold text-teal-800">Bookt</p><h1 className="text-3xl font-bold">Sign in</h1><p className="mt-2 text-sm text-stone-600">Your musician booking workspace.</p><label className="mt-6 block text-sm font-medium">Email<input name="email" type="email" required className="mt-1 w-full rounded-lg border border-stone-300 p-2" /></label><label className="mt-4 block text-sm font-medium">Password<input name="password" type="password" required className="mt-1 w-full rounded-lg border border-stone-300 p-2" /></label><button className="mt-6 w-full rounded-lg bg-teal-800 px-4 py-2.5 font-semibold text-white hover:bg-teal-900">Sign in</button></form></main>;
}
