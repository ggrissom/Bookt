import Link from "next/link";
const items = [["/", "Dashboard"], ["/venues", "Venues"], ["/opportunities", "Opportunities"], ["/bookings", "Bookings"], ["/calendar", "Calendar"], ["/tasks", "Tasks"], ["/settings/integrations", "Integrations"]] as const;
export function Shell({ children }: { children: React.ReactNode }): React.ReactNode { return <div className="min-h-screen md:grid md:grid-cols-[220px_1fr]"><aside className="border-b border-stone-300 bg-white p-4 md:min-h-screen md:border-r md:border-b-0"><Link href="/" className="text-xl font-bold text-teal-800">Bookt</Link><p className="mb-4 mt-1 text-xs text-stone-500">Musician booking workflow</p><nav aria-label="Primary" className="flex gap-1 overflow-x-auto md:flex-col">{items.map(([href, name]) => <Link key={href} href={href} className="whitespace-nowrap rounded-lg px-3 py-2 text-sm font-medium text-stone-700 hover:bg-stone-100 hover:text-teal-900">{name}</Link>)}</nav></aside><main id="main-content" className="mx-auto w-full max-w-7xl p-5 md:p-8">{children}</main></div>; }
export function PageHeader({ title, description, action }: { title: string; description: string; action?: React.ReactNode }): React.ReactNode { return <header className="mb-7 flex flex-col gap-3 border-b border-stone-300 pb-5 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-3xl font-bold tracking-tight">{title}</h1><p className="mt-1 text-sm text-stone-600">{description}</p></div>{action}</header>; }
