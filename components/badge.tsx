import { label } from "@/lib/constants";
export function Badge({ value }: { value: string }): React.ReactNode { const tone = value.includes("CONFIRMED") || value.includes("PAID") || value.includes("REPEAT") ? "bg-emerald-100 text-emerald-900" : value.includes("DECLINED") || value.includes("CANCELLED") || value.includes("DO_NOT") ? "bg-rose-100 text-rose-900" : value.includes("FOLLOW") || value.includes("WAITING") || value.includes("PENDING") ? "bg-amber-100 text-amber-900" : "bg-stone-200 text-stone-800"; return <span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-semibold ${tone}`}>{label(value)}</span>; }
